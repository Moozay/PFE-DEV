
<!-- include header -->
<%- include('includes/_headeradmin') %>
<!-- /include header -->


    <!-- Main Site -->
    <main id="site-main">
        <div class="container">
            <div class="box-nav d-flex justify-between">
                <a href="/add_facture" class="border-shadow">
                    <span class="text-gradient">NOUVEAU FACTURE <i class="fas fa-user"></i></span>
                </a>
            </div>

            <!-- form handling -->
            <form action="/" method="POST">
                <table class="table">
                    <thead class="thead-dark">
                        <tr>
                            <th>DATE</th>
                            <th>REFERENCE</th>
                            <th>NOM DE L'ENTREPRISE</th>
                            <th>PRIX TOTAL</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <%- include('includes/_showfacture') %>
                    </tbody>
                </table>
            </form>
            <div class="box-nav d-flex justify-between">
                <a onclick="link()" class="border-shadow">
                    <span class="text-gradient">DASHBOARD <i class="fas fa-user"></i></span>
                </a>
            </div>
        </div>
    </main>
<!-- /Main Site -->
<script>
    var getCookies = function(){
  var pairs = document.cookie.split(";");
  var cookies = {};
  for (var i=0; i<pairs.length; i++){
    var pair = pairs[i].split("=");
    cookies[(pair[0]+'').trim()] = unescape(pair.slice(1).join('='));
  }
  return cookies;
}

var myCookies = getCookies();
console.log(myCookies.role);

function link() {
    switch (myCookies.role) {
        case 'comptable':
            location.assign('/accueilcomptable')
            break;
        case 'directeur':
            location.assign('/accueildirecteur')
            break;
        
    
        default:
            break;
    }
}
</script>


<!-- include footer -->
<%- include('includes/_footeradmin') %>
<!-- /include footer -->